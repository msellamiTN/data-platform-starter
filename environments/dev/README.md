# DEV environment
#
# This folder is the Terraform root for the DEV environment.
# The learner creates versions.tf, provider.tf, variables.tf, main.tf and outputs.tf
# here during the training modules.

# Backend configuration template:
#   Copy backend.hcl.example to backend.hcl and fill in the values.
#   backend.hcl is gitignored and must never be committed.

# Terraform variable file template:
#   Copy terraform.tfvars.example to terraform.tfvars and fill in the values.
#   terraform.tfvars is gitignored and must never be committed.
