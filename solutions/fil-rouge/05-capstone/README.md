# Capstone — same stack as 04-day3-rbac with full documentation

Copy `04-day3-rbac/environments/dev` structure or apply directly from here.

```powershell
cd project/05-capstone/environments/dev
cp terraform.tfvars.example terraform.tfvars
terraform init
terraform apply
```

See `courses/day-03/module-12-capstone/lab.md` for evaluation checklist.
